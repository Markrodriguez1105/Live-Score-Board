server {
    listen 80;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /admin/index.html;
    }

    location /admin/ {
        try_files $uri $uri/ /admin/index.html;
    }

    location = /admin {
        return 301 /admin/;
    }
}
