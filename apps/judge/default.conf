server {
    listen 80;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /judge/index.html;
    }

    location /judge/ {
        try_files $uri $uri/ /judge/index.html;
    }

    location = /judge {
        return 301 /judge/;
    }
}
